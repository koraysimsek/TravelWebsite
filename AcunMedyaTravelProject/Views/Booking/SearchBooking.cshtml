@model List<AcunMedyaTravelProject.Entities.Booking>
@{
    ViewBag.Title = "SearchBooking";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h2>SearchBooking</h2>

<form method="get" action="/Booking/SearchBooking">
    <div class="form-group">
        <input type="text" class="form-control" style="width:256px" class="form-control" name="searchText" placeholder="Başlık veya Açıklamaya göre ara" value="@Request.QueryString["searchText"]" />
    </div>

    <button type="submit" class="btn btn-primary">Ara</button>

</form>

<br />

@if (Model.Any())
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>BookingID</th>
                <th>Başlık</th>
                <th>Açıklama</th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.BookingID</td>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                </tr>
            }

        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">Hiç bir veri bulunamadı</div>
}